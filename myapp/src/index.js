import React from 'react';
import ReactDOM from 'react-dom/client';
import Basic from './learn';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <React.StrictMode>
    <Basic />
  </React.StrictMode>
);
